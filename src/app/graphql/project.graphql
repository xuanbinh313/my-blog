query getProject($slug: String!) {
  project(slug: $slug) {
    slug
    title
    hero {
      type
      image
      title
      subtitle
      content
    }
    blocks {
      name
      value
      type
    }
  }
}
