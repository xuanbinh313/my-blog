query getBlogs {
  blogs {
    image
    title
    slug
    content
    tags {
      slug
      title
    }
    published
  }
}

query blogBySlug($slug: String!) {
  blog(slug: $slug) {
    slug
    title
    content
    image
    tags {
      slug
      title
    }
  }
}
