query getTags {
  tags {
    id
    slug
    title
    image
    content
  }
}
query getTagById($id: ID!) {
  tag(id: $id) {
    id
    slug
    title
    content
    image
    link
  }
}

mutation createTag($id: ID!, $payload: InputTag!) {
  createTag(id: $id, payload: $payload) {
    id
    slug
    title
    content
    image
    link
  }
}
